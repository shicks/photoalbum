This is a simple photo album in PHP and Javascript.  It supports:
  - Multiple albums
  - Generates thumbnails and medium-sized versions (statically)
  - Serves movies with Flash
  - Allows comments and likes (with no authentication)
